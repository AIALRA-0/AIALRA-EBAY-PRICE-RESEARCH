# eBay 官方网页真实只读验证

## 验证时间

验证日期为 `2026-07-26`

验证使用 eBay 官方网页和匿名浏览器会话

没有使用 API、搜索引擎摘要、第三方价格或账户写入

## 搜索验证

查询词为 `VITURE Beast XR glasses`

官方搜索地址成功进入结果列表，不需要登录

结果卡片可以读取数字商品编号、标题、展示价、运费文字、卖家、成色摘要、图片和详情链接

同一页面包含固定价、议价和不同成色商品，证明搜索结果必须保留购买形式和成色

## 详情验证

验证商品编号为 `366447553040`

详情页能够读取标题、图片、标准价格、优惠条件、卖家好评率、翻新成色、在售数量、历史售出数量、保修、配送和退货区域

详情页的结构化文本快照不完整，视觉页面和可交互 DOM 仍能读取关键控件

这证明具体 Skill 需要允许搜索页与详情页使用不同读取技术，同时保持相同输出 Schema

## 验证形成的模板反馈

- 打开首页不能证明搜索能力
- 搜索结果和详情需要分别验证
- 优惠价必须保存适用条件
- 拍卖当前价不能作为最终购买总价
- 同一后端第一轮成功后仍可能在后续轮次触发限制
- 后端预检状态需要进入机器可检查 Schema

## 未验证范围

- 没有执行购买、出价、议价、关注或联系卖家
- 没有进入结账页面
- 没有核验个性化税费
- 没有读取账户信息、Cookie、本地存储或浏览器配置
- 没有声称本次页面结果代表未来时间
